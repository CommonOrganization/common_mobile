import 'package:flutter/material.dart';

class ChatUploadUserSelectScreen extends StatelessWidget {
  const ChatUploadUserSelectScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
